pipeline {
    agent any
    stages {
        stage('Run Command') {
            steps {
                script {
                    // Use Windows-specific command for background execution
                    bat 'start myscript.bat'
                }
            }
        }
    }
}
